import React from 'react';

import {Container, Typography} from '../../common';

const ProfileScreen = () => {
  return (
    <Container align="center">
      <Typography subheading>ProfileScreen</Typography>
    </Container>
  );
};
export default ProfileScreen;
